<%= form_with(model: prod_category) do |form| %>
  <% if prod_category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(prod_category.errors.count, "error") %> prohibited this prod_category from being saved:</h2>

      <ul>
        <% prod_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :categoryId, style: "display: block" %>
    <%= form.number_field :categoryId %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
