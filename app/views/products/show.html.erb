<p style="color: green"><%= notice %></p>

<%# <%= render @product %> 

<div class="box">
  <% if @product.image.url %>
    <%= image_tag @product.image.url, alt:@product.image.url %>
  <% end %>

  <div class="title"><%= @product.manufacturer + " - " + @product.name %></div>

  <div class="subtitle"><%= number_to_currency(@product.price) %></div>

  <div class="content"><%= @product.description %></div>

  <div class="content"><strong>Weight: </strong> <%= @product.weight %>oz</div>

  <div class="content"><strong>Stock on hand: </strong> <%= @product.stock %> units</div>


  <p><%= link_to 'Add to Cart', add_to_cart_path(@product.id), method: :post %></p>

  <br>
  
  <%= social_share_button_tag(@product.name) %>
  <%# <p><%= link_to 'Add to Cart', cart_line_items_path(product_id: @product.id), method: :post %>

  <%# <% if current_user %>
    <%# <% if current_user.id === 1 %>
      <%# <div>
        <%= link_to "Edit this product", edit_product_path(@product) %> 
        <%# <%= link_to "Back to products", products_path %> 

        <%# <%= button_to "Destroy this product", @product, method: :delete %> 
      <%# </div> %> 
    <%# <% end %> 
  <%# <% end %>  

</div>